name: "AppImage Windows"

# manual trigger
on:
  workflow_dispatch:

env:
  ARCH: "x86_64"

jobs:
  AppImage_Ubuntu22-04:
    name: AppImage Ubuntu 22.04 Qt 6.2
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set global env
      run: |
       echo "BASE_DIR=$(pwd)" >> $GITHUB_ENV
       echo "LINUXDEPLOY_OUTPUT_VERSION=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

    - name: Install Dependencies
      run: sudo apt update && sudo apt install qmake6 qt6-base-dev qt6-l10n-tools qt6-tools-dev qt6-tools-dev-tools qt6-declarative-dev libqt6svg6-dev qt6-wayland libgl-dev libfuse2 appstream

    - name: Download linuxdeploy
      run: |
       wget -c -nv "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-"$ARCH".AppImage"
       chmod a+x linuxdeploy-"$ARCH".AppImage
       LINUXDEPLOY_BIN=${PWD}/linuxdeploy-"$ARCH".AppImage

    - name: Download linuxdeploy-plugin-appimage
      run: |
       wget -c -nv "https://github.com/linuxdeploy/linuxdeploy-plugin-appimage/releases/download/continuous/linuxdeploy-plugin-appimage-"$ARCH".AppImage"
       chmod a+x linuxdeploy-plugin-appimage-"$ARCH".AppImage

    - name: Download linuxdeploy-plugin-qt
      run: |
       wget -c -nv "https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-"$ARCH".AppImage"
       chmod a+x linuxdeploy-plugin-qt-"$ARCH".AppImage

    - name: Download ffsend
      run: |
       FFSEND_VERSION=$(curl --silent https://api.github.com/repos/timvisee/ffsend/releases/latest | sed -Ene '/^ *"tag_name": *"(v.+)",$/s//\1/p')
       wget -c -nv "https://github.com/timvisee/ffsend/releases/download/$FFSEND_VERSION/ffsend-$FFSEND_VERSION-linux-x64-static"
       mv ./ffsend-* ./ffsend
       chmod a+x ./ffsend

    - name: Compile
      run: |
       cmake -B build-cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_PREFIX_PATH=/usr/include/x86_64-linux-gnu/qt6/
       cmake --build build-cmake -- -j$(nproc)
       DESTDIR=${PWD}/AppDir cmake --install build-cmake

    - name: Create AppImage
      run: |
       mkdir -p ${BASE_DIR}/out
       export EXTRA_PLATFORM_PLUGINS="libqwayland-generic.so"
       export EXTRA_QT_MODULES="waylandcompositor"
       export QMAKE=/usr/lib/qt6/bin/qmake
       ${PWD}/linuxdeploy-"$ARCH".AppImage --appdir ${PWD}/AppDir --plugin qt --output appimage
       mv -v StackAndConquer-*.AppImage ${BASE_DIR}/out

    - name: Upload AppImage
      run: |
       ./ffsend upload ${BASE_DIR}/out/StackAndConquer-*.AppImage

  Windows_msvc:
    name: Win latest Qt 6 msvc
    runs-on: windows-2022
    env:
      QT_VERSION: '6.10.3'
      BUILD: 'msvc2022_64'
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ env.QT_VERSION }}
        cache: 'true'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2022_64'
    
    - name: Compile
      run: |
       mkdir build-cmake
       cmake -G "Visual Studio 17 2022" -S ${{github.workspace}} -B ${{github.workspace}}/build-cmake
       cmake --build ${{github.workspace}}/build-cmake --config Release
    
#    - name: Create Package
#      env:
#        VCINSTALLDIR: 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC'
#      shell: cmd
#      run: |
#       git rev-parse --short main > buildnr.txt
#       set /p buildnr= < buildnr.txt
#       del buildnr.txt
#       mkdir StackAndConquer\cpu
#       mkdir StackAndConquer\boards
#       copy build-cmake\Release\StackAndConquer.exe StackAndConquer\StackAndConquer.exe
#       windeployqt --release --no-translations --no-opengl-sw StackAndConquer\StackAndConquer.exe
#       copy COPYING StackAndConquer\
#       curl -fsSL -o ReadMe.txt https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/ReadMe.txt
#       copy ReadMe.txt StackAndConquer\
#       xcopy /i /e /s data\cpu\*.js StackAndConquer\cpu\
#       xcopy /i /e /s data\boards\* StackAndConquer\boards\
#       7z a -tzip -mx=7 StackAndConquer-%buildnr%-Windows_${{ env.BUILD }}.zip .\StackAndConquer\
#       curl -fsSL -o stackandconquer.nsi https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/stackandconquer.nsi
#       curl -fsSL -o removeprevious.nsh https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/removeprevious.nsh
#       curl -fsSL -o stackandconquer.ico https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/stackandconquer.ico
#       makensis stackandconquer.nsi
#       ren StackAndConquer_Installer.exe StackAndConquer-%buildnr%-Windows_${{ env.BUILD }}.exe
#       
#    - uses: actions/upload-artifact@v4
#      with:
#        name: StackAndConquer-Windows_Artefacts
#        path: StackAndConquer-*.*
#        retention-days: 1
#        overwrite: true

  Windows_mingw:
    name: Win 2022 Qt 6 mingw
    runs-on: windows-2022
    env:
      QT_VERSION: '6.10.3'
      BUILD: 'mingw1310_64'
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ env.QT_VERSION }}
        cache: 'true'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_mingw'
        tools: 'tools_mingw1310'

    - name: Add Qt MinGW to PATH
      run: echo "${{ env.QT_ROOT_DIR }}/../Tools/${{ env.BUILD }}/bin" >> $GITHUB_PATH

    - name: Compile
      run: |
       mkdir build-cmake
       cmake -G "MinGW Makefiles" -S ${{github.workspace}} -B ${{github.workspace}}/build-cmake
       cmake --build ${{github.workspace}}/build-cmake --config Release
    
    - name: Create Package
      shell: cmd
      run: |
       git rev-parse --short main > buildnr.txt
       set /p buildnr= < buildnr.txt
       del buildnr.txt
       mkdir StackAndConquer\cpu
       mkdir StackAndConquer\boards
       copy build-cmake\StackAndConquer.exe StackAndConquer\StackAndConquer.exe
       windeployqt --release --no-translations --no-opengl-sw StackAndConquer\StackAndConquer.exe
       copy COPYING StackAndConquer\
       curl -fsSL -o ReadMe.txt https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/ReadMe.txt
       copy ReadMe.txt StackAndConquer\
       xcopy /i /e /s data\cpu\*.js StackAndConquer\cpu\
       xcopy /i /e /s data\boards\* StackAndConquer\boards\
       7z a -tzip -mx=7 StackAndConquer-%buildnr%-Windows_${{ env.BUILD }}.zip .\StackAndConquer\
       curl -fsSL -o stackandconquer.nsi https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/stackandconquer.nsi
       curl -fsSL -o removeprevious.nsh https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/removeprevious.nsh
       curl -fsSL -o stackandconquer.ico https://raw.githubusercontent.com/ElTh0r0/stackandconquer/packaging/Windows/stackandconquer.ico
       makensis stackandconquer.nsi
       ren StackAndConquer_Installer.exe StackAndConquer-%buildnr%-Windows_${{ env.BUILD }}.exe
       
    - uses: actions/upload-artifact@v7
      with:
        name: StackAndConquer-Windows_Artefacts
        path: StackAndConquer-*.*
        retention-days: 2
        overwrite: true
